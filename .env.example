# Copy to .env and set strong values. Used by docker-compose.yml for the Postgres
# container, the reel-vex app's connection string, and the admin token that gates
# the ingest-trigger endpoint (POST /v1/ingest).
POSTGRES_PASSWORD=change-me
ADMIN_TOKEN=change-me
